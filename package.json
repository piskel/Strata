{
  "name": "strata",
  "version": "0.0.1",
  "description": "A serverless file-sharing web app.",
  "main": "index.js",
  "scripts": {
    "compile": "concurrently \"npx tsc --project ./\" \"copyfiles -e \"./**/*.ts\" --error -u 1 ./public/**/* ./dist/\" \"copyfiles --error -u 3 ./node_modules/ipfs-core/dist/index.min.js ./dist/js/ipfs\"",
    "watch": "nodemon --watch public --ext * -x \"npm run compile\"",
    "clean": "rimraf dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/piskel/strata.git"
  },
  "author": "Piskel",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/piskel/strata/issues"
  },
  "homepage": "https://github.com/piskel/strata#readme",
  "dependencies": {
    "ipfs-core": "^0.5.4",
    "nodemon": "^2.0.7"
  },
  "devDependencies": {
    "concurrently": "^6.0.2",
    "copyfiles": "^2.4.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.4"
  }
}
